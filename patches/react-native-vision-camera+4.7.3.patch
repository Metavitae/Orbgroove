diff --git a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/OrientationManager.kt b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/OrientationManager.kt
index 5326d6b..ebfd851 100644
--- a/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/OrientationManager.kt
+++ b/node_modules/react-native-vision-camera/android/src/main/java/com/mrousavy/camera/core/OrientationManager.kt
@@ -3,6 +3,7 @@ package com.mrousavy.camera.core
 import android.content.Context
 import android.hardware.display.DisplayManager
 import android.util.Log
+import android.view.Display
 import android.view.OrientationEventListener
 import android.view.Surface
 import com.mrousavy.camera.core.types.Orientation
@@ -18,8 +19,13 @@ class OrientationManager(private val context: Context, private val callback: Cal
   private var lastPreviewOrientation: Orientation? = null
 
   // Screen Orientation Listener
-  private var screenRotation = Surface.ROTATION_0
   private val displayManager = context.getSystemService(Context.DISPLAY_SERVICE) as DisplayManager
+
+  // Seed from the display's real rotation. Starting at ROTATION_0 and waiting for
+  // onDisplayChanged means an app that launches already in landscape (orientation
+  // lock) never receives a change event, so previewOrientation stays "portrait" and
+  // the preview renders a quarter-turn off.
+  private var screenRotation = displayManager.getDisplay(Display.DEFAULT_DISPLAY)?.rotation ?: Surface.ROTATION_0
   private val displayListener = object : DisplayManager.DisplayListener {
     override fun onDisplayAdded(displayId: Int) = Unit
     override fun onDisplayRemoved(displayId: Int) = Unit
